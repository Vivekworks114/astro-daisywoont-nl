You are a Senior Astro.js Migration Engineer and WordPress Content Migration Specialist.

WEBSITE URL:
https://daisywoont.nl

WORDPRESS XML EXPORT (local path):
C:\Users\Asus\Downloads\pipeline_complete\jobs\daisywoont.nl\wordpress_export.xml

GITHUB REPOSITORY:
https://github.com/Vivekworks114/astro-daisywoont-nl

ASTRO PROJECT (local path):
C:\Users\Asus\Downloads\pipeline_complete\jobs\daisywoont.nl\astro_project

---

## OBJECTIVE

Migrate **https://daisywoont.nl** from WordPress to a production-ready Astro.js site.
Use the **live website** as the visual and structural reference. Use the **XML** as the content source of truth.

**Quality bar: MATCH OR EXCEED the live site. Never downgrade.**
- Same layout, navigation, footer, colors, typography, spacing
- You MAY improve performance, accessibility, and code quality
- You MUST NOT remove sections, columns, links, or pages that exist on the live site
- Every link on the live site must work on the Astro site (200 or valid external URL)

---

## PHASE 1 — Frontend shell (shared on EVERY page)

Analyze https://daisywoont.nl completely before writing code.

Build these Astro components used on **all** routes:
- `Layout.astro` — loads `src/styles/global.css` on every page
- `Header.astro` — logo, search, navigation
- `Navigation.astro` — desktop dropdown + mobile menu
- `Footer.astro` — **exact 6-column footer** (see FOOTER SPEC below)
- `Hero`, `Services`, `Features`, `Testimonials`, `CTA`, `Contact` as needed

### Global rules
1. Download ALL images from https://daisywoont.nl → `public/images/` (never hotlink)
2. Mobile-first, 320px–1920px, no horizontal scroll
3. **One Header and one Footer only** — never duplicate inside page body HTML
4. Create `/blog/` listing and `/blog/[slug]/` detail pages
5. Store reusable link data in `src/data/` (e.g. `nav-from-wp.json`, `footer-links.ts`, `site.ts`)

---

## PHASE 2 — Content migration (from XML)

1. Read the XML at `C:\Users\Asus\Downloads\pipeline_complete\jobs\daisywoont.nl\wordpress_export.xml`.
2. Migrate every post, page, category, menu item, and author.
3. Blog posts → `src/content/blog/*.md` (slug, date, author, categories, tags, image, content).
4. Replace every WordPress media URL with local `/images/...`.
5. Preserve URL paths exactly (trailing slash):
   - `/over-ons/` → `/over-ons/`
   - `/blog/post-slug/` → `/blog/post-slug/`
   - `/beste-product-slug/` → `/beste-product-slug/`
   - `/slaapkamer/`, `/meubilair/`, `/veiligheid/`, etc.

### HTML page rules (CRITICAL)
When migrating WordPress/Elementor HTML pages:
- **Extract MAIN CONTENT ONLY** (`<main>`, `#content`, `.elementor-location-single`)
- **STRIP completely:** `<body>`, `<header>`, `<footer>`, Elementor header/footer, skip-links, duplicate nav, duplicate search, author footer blocks, social link dumps
- **Never** dump full page HTML with `set:html` if it contains header/footer/nav
- Remove placeholders: `ddd`, `[zb_mp_*]`, `href="#"`, empty `src`
- Fix all internal links to local paths (`/beste-*/`, `/blog/*/`, category pages)

### Page types to create (all must exist and return 200)
| Type | Route pattern |
|------|---------------|
| Home | `/` |
| Blog list | `/blog/` |
| Blog post | `/blog/[slug]/` |
| Category | `/[category]/` |
| Product review | `/beste-[slug]/` |
| Static | `/over-ons/`, `/contact/` |
| Sitemap | `/sitemap/` |
| Redirect | `/blogs/` → `/blog/` |

---

## FOOTER SPEC — must match live site exactly

Rebuild `Footer.astro` using `src/data/footer-links.ts`. Crawl https://daisywoont.nl footer and include **every link**.

### 6 columns on desktop
1. **Brand** — logo, full description, Sitemap → `/sitemap/`, social icons (Facebook, Instagram, Twitter/X, YouTube), copyright year
2. **Informatie** — Home, Blog, Over ons, Contact
3. **Andere artikelen** — 5 most recent blog posts → `/blog/{slug}/`
4. **Verwarmers & Koelers** — all product links from live footer → `/beste-{slug}/`
5. **Wassen & Drogen** — all product links from live footer → `/beste-{slug}/`
6. **Andere Daisy sites** — external links (new tab): daisykookt.nl, daisybeautytips.nl, daisytuiniert.nl, outdoordaisy.nl, daisyverbouwt.nl, lifestyledaisy.nl

Dark background, white/gray text. Same footer on every page via Layout.

---

## NAVIGATION SPEC

- Build from XML menu → `src/data/nav-from-wp.json`
- Blogs → `/blog/` (not `/blogs/`)
- Every dropdown child → `/beste-{slug}/` (internal path)
- Parent categories → `/slaapkamer/`, `/meubilair/`, `/veiligheid/`
- Mobile: tap parent = navigate; chevron = open submenu
- Desktop: hover dropdown
- **Zero 404s** from nav clicks

---

## PHASE 3 — Link & image audit (mandatory before DONE)

1. `npm run build` — zero errors
2. Verify Header nav, Footer (all 6 columns), homepage cards, 5 sample product pages
3. Every internal link returns HTTP 200 on `astro preview`
4. Every image `src` resolves — no broken images
5. No duplicate header/footer in page body
6. No giant bullet-list nav in body content

Fix ALL broken links and images before finishing.

---

## GIT — commit and push

```bash
git add -A
git commit -m "Migrate https://daisywoont.nl to Astro — full parity with live site"
git push origin main
```

If push fails, fix and retry until push succeeds.

---

## DONE criteria

Write exactly **DONE** only when ALL are true:
- Build passes with 0 errors
- Page count matches live site (90+ pages)
- Footer has 6 columns with ALL links from live site
- Header nav + all dropdown links work
- No broken images
- No WP header/footer duplicated in page content
- UI matches live site (no unstyled raw HTML pages)
- Code pushed to https://github.com/Vivekworks114/astro-daisywoont-nl
